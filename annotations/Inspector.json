{
  "component": "Inspector",
  "category": "Panels",
  "archetype": "composite",
  "intent": "The composable right-rail shell. Renders the PanelSection children it is given, in order.",
  "guidance": "Compose the inspector from PanelSections rather than hardcoding rows — reorder, add, remove, or replace a section by changing children, and nothing else moves. The header stays fixed while the section stack scrolls beneath an overlay scrollbar (no layout shift). Label the rail with aria-label so it reads as a complementary landmark, and let each PanelSection own its own region.",
  "anatomy": [
    { "part": "Header", "description": "Fixed zone above the scroll — e.g. the zoom control and the Design / Animate tab row." },
    { "part": "Section stack", "description": "The PanelSection children, rendered in the order given; the composition surface." },
    { "part": "Scroll region", "description": "Overlay scrollbar that hides with no layout shift; only the selected content sits in the reading order." }
  ],
  "use_when": [
    "Building an inspector by composing sections.",
    "You want to reorder, add, remove, or replace a section by changing children."
  ],
  "dont_use_when": [
    "You want the capability-driven default panel — use PropertyPanel."
  ],
  "variants": {
    "header": [
      "optional fixed zone above the scroll area (e.g. zoom + Design/Animate tabs)"
    ]
  },
  "slots": {
    "header": "Fixed zone above the scrolling sections (e.g. the zoom + Design/Animate tab row).",
    "children": "The PanelSection stack, rendered in the order given."
  },
  "states": [
    "default",
    "scrolled"
  ],
  "tokens": {
    "surface": {
      "bg": "bg/default",
      "ring": "border/translucent"
    },
    "section": {
      "header height": "40px",
      "gutter": "16px"
    }
  },
  "a11y": {
    "role": "complementary (labelled by aria-label)",
    "sections": "each PanelSection can opt into a region landmark.",
    "scroll": "overlay scrollbar — hides with no layout shift; only the selected content is in the reading order."
  },
  "enforce": {
    "role": "complementary",
    "ariaRole": true,
    "tokensOnly": true
  },
  "code": {
    "import": "import { Inspector, PanelSection } from '@composa/ui'",
    "example": "<Inspector aria-label=\"Design inspector\" header={<Tabs … />}>\n  <PanelSection title=\"Position\">…</PanelSection>\n  <PanelSection title=\"Fill\">…</PanelSection>\n</Inspector>"
  }
}
